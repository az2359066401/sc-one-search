spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sc-one-search/sc-one-search-config-server
          search-paths: config-file
          username:
          password:
      label: master
server:
  port: 8767